---
apiVersion: v1
stringData:
  secret: ${PROXMOX_SECRET=""}
  token: ${PROXMOX_TOKEN=""}
  url: ${PROXMOX_URL=""}
kind: Secret
metadata:
  name: manager-credentials
  labels:
    platform.ionos.com/secret-type: "proxmox-credentials"
